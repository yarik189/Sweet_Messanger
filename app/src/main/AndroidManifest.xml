<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kanefron5.com.sweetmessanger" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Messages"
            android:label="Сообщения"

            android:noHistory="true">

            </activity>
        <activity android:name=".Friends"
            android:label="Друзья"

            android:noHistory="true">

        </activity>
<activity android:name=".Chaing"
    android:label="Список изменений"
    android:theme="@style/Chaing">

    </activity>
<activity android:name=".Settings"
    android:label="Настройки"
    android:theme="@style/App"
    android:noHistory="true">
    </activity>

    </application>

</manifest>
